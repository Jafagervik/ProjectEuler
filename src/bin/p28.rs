use itertools::Itertools;
use rayon::prelude::*;

fn solve() -> usize {
    let mut dim = 3;
    let mut sum = 1;

    while dim <= n {
        dim += 2
    }

    sum
}

fn main() {
    let sol = solve();

    println!("Solution: {:?}", sol);
}
